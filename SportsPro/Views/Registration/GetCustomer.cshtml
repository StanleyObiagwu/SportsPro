@model RegisterViewModel

<h2>Get Customer</h2>

<form asp-controller="Registration"  asp-action="Registrations">
    @*<div asp-validation-summary="All" class="text-danger"></div>*@


    <div class="form-group">

        <label asp-for="@Model.CustomerID">Customer</label>
        <select asp-for="@Model.CustomerID" class="form-control"
                asp-items="@(new SelectList(Model.Customers, "CustomerID", "FullName", 0))">
            <option value="">Please select one</option>
            @*@foreach (Customer customer in Model.Customers)
            {
                <option value="@customer.CustomerID">@customer.FullName</option>
            }*@
        </select>
        @*<span asp-validation-for="@Model.CustomerID"></span>*@

    </div>


    <div>
        <button type="submit" class="btn btn-dark" asp-controller="Registration"
                asp-action="Registrations">Select
        </button>

        <a asp-controller="Home" asp-action="Index"
           class="btn btn-dark">Cancel</a>
    </div>
</form>











